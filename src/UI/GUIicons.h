#pragma once

/** Useful stuff for creating controls that use icons.*/

enum RadioOrientationType {lblFirst, iconFirst};

const int iconOffset = 20;
const int defaultIconset = 0;
const int defaultIconButtonSize = 16;

const int leftIconArrow = 5;
const int rightIconArrow = 4;
